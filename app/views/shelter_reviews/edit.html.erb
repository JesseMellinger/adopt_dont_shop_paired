<%= render partial: "shelter_review_form", locals: {
                                                   path: "/shelters/#{@shelter.id}/#{@review.id}",
                                                   method: :patch,
                                                   submit_text: 'Update Review',
                                                   title: @review.title,
                                                   rating: @review.rating,
                                                   content: @review.content,
                                                   picture: @review.picture,
                                                   name: @user.name
} %>
